# Test artifacts (root-level only)
/reports
/coverage
/test-results

# Stryker temp (root-level only)
/.stryker-tmp

# AI/IDE artifacts (root-level only)
/.engram
/.atl
/engram-backup-*.json

# Docs (root-level only)
/docs
/openspec
*.md

# CI configs (root-level only)
/.github
/.circleci
/.githooks

# DB configs (root-level only)
/supabase
/sql

# E2E tests (root-level only)
/tests
/playwright.config.js

# Stryker config (root-level only)
/stryker.config.json
/stryker-run.log

# Mutation scripts (root-level only)
/scripts/run-mutation-*.sh

# Extras
/nul
/.gga
